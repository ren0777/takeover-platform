{
  "name": "@takeover/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:workspace-deps": "pnpm --filter @takeover/shared build",
    "predev": "pnpm run build:workspace-deps",
    "dev": "next dev --port 3000",
    "prebuild": "pnpm run build:workspace-deps",
    "build": "next build",
    "start": "next start --port 3000",
    "pretypecheck": "pnpm run build:workspace-deps",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "pretest": "pnpm run build:workspace-deps",
    "test": "vitest run",
    "territory:contract-smoke": "node scripts/territory-contract-smoke.ts"
  },
  "dependencies": {
    "@takeover/shared": "workspace:*",
    "next": "15.5.24",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@next/eslint-plugin-next": "15.5.24",
    "@tailwindcss/postcss": "4.3.3",
    "@takeover/config": "workspace:*",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "jsdom": "^30.0.1",
    "tailwindcss": "4.3.3",
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
